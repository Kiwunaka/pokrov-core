# Security

Use GitHub private vulnerability reporting for security issues.

Do not attach production profiles, credentials, private keys, WARP registration data, customer information, or unredacted runtime logs to a public issue. Reproduce problems with synthetic fixtures and include only the minimum logs needed to identify the failing component.

Supported security fixes target the current major release. Older releases receive fixes only when the release owner explicitly keeps that line active.
